import { StyleRules, Theme } from '@material-ui/core';

const RescheduleBookingStyles = (_theme: Theme): StyleRules<any, any> => ({
  formControl: {
    width: '100%',
  },
});

export { RescheduleBookingStyles };

