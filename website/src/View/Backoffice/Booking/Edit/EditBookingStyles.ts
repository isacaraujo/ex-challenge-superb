import { StyleRules, Theme } from '@material-ui/core';

const EditBookingStyles = (theme: Theme): StyleRules<any, any> => ({
  formControl: {
    width: '100%',
  },
});

export { EditBookingStyles };
