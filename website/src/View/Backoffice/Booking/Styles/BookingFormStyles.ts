import { StyleRules, Theme } from '@material-ui/core';

const BookingFormStyles = (theme: Theme): StyleRules<any, any> => ({
  formControl: {
    width: '100%',
  },
});

export { BookingFormStyles };
